# {HOST} is a placeholder substituted at container start with the runtime
# HOST env var (see root docker-compose.yml). Leave HOST empty to use a
# relative API path (e.g. "/api/v1") instead of a full domain.
VITE_API_URL={HOST}/api/v1
VITE_STATS_KEY=UMAMI API KEY